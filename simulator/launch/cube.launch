<launch>
    <!-- Launch Gazebo -->
    <include file="$(find gazebo_ros)/launch/empty_world.launch"/>

    <!-- Delay the spawn script to allow Gazebo to initialize -->
    <node name="spawn_cube" pkg="simulator" type="spawn_cube.py" output="screen"/>
</launch>
