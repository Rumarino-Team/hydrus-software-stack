opencv-python
numpy
matplotlib
requests
colorama
pyserial

# Note: Additional dependencies can be installed as optional extras:
# pip install -e .[web]              # For FastAPI/Flask web services
# pip install -e .[depth-estimation] # For depth estimation with Depth-Anything-ONNX
# pip install -e .[dev]              # For development tools (black, isort, etc.)
# pip install -e .[all]              # Install all optional dependencies
